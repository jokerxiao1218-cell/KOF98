# 拳皇98复刻(特瑞篇)设计文档

> 创建时间:2026-09-10 | 状态:**开发中(0/A/B/C/E/F/F2/G ✅ 已推 GitHub(G 截图待用户过目);素材系统 v2 闭环,剩 D 调参交付)**

一句话:用 Pygame 复刻 KOF98 的 1v1 格斗体验,第一版只做特瑞·伯格一个角色,玩家搓招打入门 AI,三局两胜。

---

## 1. 需求卡:拳皇98复刻·单角色版

- **背景**:全新项目 `~/kof98`。复刻 KOF98(拳皇98,SNK 1998 年的街机格斗游戏)的 1v1 格斗手感,第一版只做特瑞·伯格(Terry Bogard)一个角色。延续 mota50 / sheepandsheep 的项目模式:Pygame + 程序自绘素材 + pytest 全覆盖 + 中文设计文档驱动。GitHub 私仓 `git@github.com:jokerxiao1218-cell/KOF98.git` 已建好(SSH 访问已验证,确认是空仓库)。
- **目标**:`./run.sh` 一条命令拉起窗口,玩家用键盘操控特瑞,与入门 AI 镜像对战(AI 用换色版特瑞,红马甲→蓝马甲),三局两胜,60fps 流畅。
- **输入/输出**:
  - 输入(键盘,P1):方向 `WASD`(W上 S下 A左 D右——注意"前/后"按角色朝向自动换算),攻击四键 `U`(轻拳A) `I`(轻脚B) `O`(重拳C) `P`(重脚D);`Enter` 开始/确认,`Esc` 暂停,`F1` 显示判定框(调参用)
  - 输出(画面):960×672 窗口(内部逻辑分辨率 320×224 ×3 整数放大,还原街机观感),HUD 含体力条/倒计时/能量槽/回合胜点/连段数
  - 具体例子:开局 P1 在舞台左侧面朝右,按住 `D` 向前走(2.4像素/帧),`↓↘→`(依次按 S、S+D、D)+`O` 出能量波(飞行道具),命中对手扣 90 血、对手进入 16 帧受击硬直;对手贴墙防御时攻击方被往后推(不许把人推出墙)
- **约束**:
  - 依赖:**零新增**,只有 pygame 2.6.1 + pytest(照先例项目 .venv 自装)
  - 版权:**零外部素材**,人物/背景/HUD 全部程序自绘,致敬造型不抄原画;音效第一版不做(静音)
  - 错误处理:数据文件缺失/数值非法 → 启动时报 `DataError`(带字段路径)退出,不许静默;窗口失焦 → 自动暂停;关窗 → 干净退出
  - 改动范围:全新项目,只动 `~/kof98`
  - 性能:逻辑 60fps 帧锁(所有数值以"帧"为单位),单帧逻辑+渲染 < 8ms
- **验收标准**:
  - 测试:pytest 全绿(计划见第 6 节,以覆盖为准,预计 ≥100 条)
  - 脚本验收:`scripts/demo.py` AI 自对弈一整局,确定性(同种子同结果),正常出 KO 判定,退出码 0
  - 真机(真人试玩):第 9 节清单全部勾选,重点是**搓招成功率**和手感
- **第一版明确不做**(防止范围膨胀):音效/BGM、第二超必杀(High Angle Geyser)、Buster Wolf(那是1999年《饿狼Mark of the Wolves》的招,98 里没有)、ADV/EX 双模式、防御崩溃、前后滚动、目押取消之外的精细系统、第二个人类玩家位、双人对战

---

## 2. 现场调查

两个先例项目高度同构(sheepandsheep 设计文档自述"与 mota50 同构"),惯例一致,kof98 直接照搬:

- **最相似既有实现**:mota50(动作游戏,有帧概念、碰撞、HUD)+ sheepandsheep(渲染/素材/测试风格)。风格、错误处理、测试写法照它们来。
- **结构惯例**:
  - 包名统一叫 `game/`,`main.py` 放 `game/main.py`,开头带"双跑法兼容"样板(`sys.path` 补仓库根,`python game/main.py` 和 `python -m game.main` 都能跑,参考 `~/sheepandsheep/game/main.py:5-8`)
  - 测试在根目录 `tests/test_<被测模块>.py`;辅助脚本在 `scripts/`;没有 pyproject/requirements,`.venv` 自带并在 README 说明
  - 纯逻辑进 `game/core/`(零 pygame import,可独立单测),渲染进 `ui.py`,素材自绘进 `assets.py`——本项目的"core 纯逻辑"纪律和格斗游戏的帧逻辑天然契合
- **测试惯例**:
  - pytest,裸 `assert` + 中文注释讲"为什么这条断言在";`@pytest.mark.parametrize` 表驱动;fixture 少量、`scope="module"` 一次性装真实数据
  - headless 套路:测试文件**顶部、import pygame 之前** `os.environ.setdefault("SDL_VIDEODRIVER", "dummy")`(+ `SDL_AUDIODRIVER=dummy`),参考 `~/sheepandsheep/tests/test_assets.py:8-9`
  - dummy 驱动与 `pygame.SCALED` 冲突的坑,mota50 已有解法(dummy 下自动去掉 SCALED,`~/mota50/game/ui.py:104`),照搬
  - **像素采样断言代替肉眼看图**:`get_at` 断言颜色/非空像素数,两个项目都是标配(参考 `~/mota50/tests/test_engine.py:945-1003`)。本项目延续,并且**红线:任何 PNG 一律不 Read 进会话**(mota50 的教训),截图验收一律走像素统计脚本
  - 特色:契约独立复算——测试里把公式独立实现一遍,不 import 生产代码,防"实现和测试一起错",照搬
- **主循环惯例**:`clock.tick(60)` + 场景栈(sheepandsheep 的 State 模式 + 下推自动机,ResultScene 还会定格底图);`App.step(events, dt)` 单帧驱动供无头测试注入事件(`~/sheepandsheep/game/ui.py:390-395`)——kof98 必须**强制要求**这个模式,格斗逻辑要能一帧一帧喂
- **素材惯例**:零图片文件、运行时程序自绘(mota50 用 16×16 字符画 + palette,`_paint()` 对非法字符画直接 raise 不许静默画歪);字体内置 OFL 文件放 `game/assets/fonts/`(sheepandsheep:ZCOOLKuaiLe + NotoEmoji + OFL 协议文本,相对 `__file__` 定位不依赖 CWD,还有 `check_font_health()` 启动自检防静默回退)。kof98 复用同款字体文件(OFL 许可允许复制)
- **数据惯例**:mota50 的帧数据/地图不是手写的,是 `game/data/*.json` + loader 校验。kof98 的招式帧表/系统参数/AI 权重全部走 `game/data/*.json` + `core/data.py` 校验——**调参不动逻辑代码**
- **文档惯例**:中文设计文档放仓库根,是"进度唯一事实来源";README 中文大白话(玩法→快速开始→依赖→按键表→架构树→已知差异);提交信息中文前缀式:`init:` / `docs:` / `batchN:` / `修复:` / `反馈迭代:`,一句话概要+测试数量(如"batch3: 三道具,72测试绿")
- **新代码落位**:照上述分层——帧逻辑/判定/输入/AI 全部落 `game/core/`(纯逻辑),素材装配 `assets.py + art/`,窗口/场景/HUD `ui.py`,数值 `game/data/*.json`

---

## 3. 业界调研

- **Ikemen GO / MUGEN(开源格斗引擎,github.com/ikemen-engine/Ikemen-GO)**——思路:帧驱动的确定性 60fps 模拟(tick-based)、"角色=状态机 + 状态控制器"、数据文件与逻辑代码分离(.def/.cns/.cmd/.air 各管各的)、受击反应做成公共状态族(5000 号段)人人共用;框架逻辑:角色/弹体/特效共用同一实体模型。**可借鉴点**:①一切以"帧"为单位、每帧一个 tick 的确定性模拟——kof98 的逻辑核心照此设计,天然可测试可回放;②数据/逻辑彻底分离;③"受击状态族"独立成状态机分支。**不照搬**:MUGEN 的脚本解释器和通用格式是为"任意角色"设计的,我们只有一个角色,Python 原生状态机即可
- **格斗游戏输入系统(社区共识)**——实现思路:输入缓冲 6~12 帧(搓完招稍后按拳仍有效)、按"方向状态变化"记录历史而不是每帧采样、宽容规则(允许缺对角线:↓→ 直接算 ↓↘→)、整条指令要在时间窗内完成、蓄力另算。⚠️ 说明:本次会话的在线检索工具两次返回异常,这部分来自引擎社区通识与 Ikemen/MUGEN 文档线索,**标注"部分未经在线核实"**,数值(窗宽、缓冲帧数)进 `system.json` 当调参项,试玩校准
- **KOF98 特瑞数据**——指令表经多源检索一致,采信:能量波 `↓↘→+A/C`、燃烧拳 `↓↙←+A/C`、爆裂踢 `↓↙←+B/D`、Power Dunk `→↓↘+B/D`、升龙撞 `↓蓄↑+A/C`、超必杀能量喷泉 `↓↘→↓↘→+A/C`。**检索到一处错误已剔除**:有结果称 98 有 Buster Wolf,那是《饿狼Mark of the Wolves》(1999)才有的招,98 不采用。**帧数据拿不到权威源**:supercombo.gg 的特瑞页(dreamcancel wiki 迁移后的家)被 Cloudflare 反爬挡死(WebFetch 两次 403,curl 带浏览器 UA + MediaWiki raw 接口均返回 JS 验证页)——因此附录 A 帧数表全部是**基于 KOF 手感的近似基线,标注"推测,待验证"**,集中放 JSON 里,batch D 试玩调参。第二超必杀 High Angle Geyser 的存在有线索但指令未核实,第一版砍掉(见需求卡)
- **特瑞形象配色(检索确认)**:红帽子(正面 FATAL FURY 字样,像素画用白色小块示意)、金长发、红色无袖马甲背心、裸臂+红色指虎拳套、蓝色牛仔裤、红白运动鞋——红白蓝的"美国味"配色,像素画按此调色板,P2 用蓝马甲换色版(经典换色做法)
- **结论**:主要参考 Ikemen/MUGEN 的"帧驱动确定性模拟 + 数据/逻辑分离"做骨架;输入系统按社区共识设计但数值参数化;角色数据用"检索核实的指令 + 近似的帧数基线";本项目约束(单角色、320×224 像素风、零图片文件)决定不引入任何通用引擎格式,Python 原生实现

---

## 4. 设计方案

### 4.1 关键设计选择(每条配为什么)

| # | 选择 | 为什么 |
|---|---|---|
| 1 | **逻辑分辨率 320×224,×3 放大到 960×672** | 还原 Neo Geo 街机原版观感;整数放大像素风干净;SCALED 冲突坑已有解法 |
| 2 | **一切以帧为单位,逻辑帧锁 60fps,不用 dt 做游戏数值** | 格斗游戏的帧数据(出招3帧判定2帧收招8帧)是绝对概念,必须帧驱动才能确定、可测试(Ikemen/MUGEN 同款思路);`clock.tick(60)` 只当节拍器,dt 不进公式 |
| 3 | **判定框系统:攻击框/受击框分离,数据进 JSON** | 格斗判定的业界标准;和画面解耦,调框不动画(F1 可视化) |
| 4 | **角色=显式状态机(FSM)+ 每帧 tick** | 攻击/受击/硬直的规则天然是状态;所有状态转移表驱动,单测可逐帧断言 |
| 5 | **输入=事件边沿+缓冲+滑动窗指令识别,方向相对化** | 搓招的本质是"短时间内的方向序列";相对化后 P1/P2 同一管线(镜像自动成立);社区共识做法 |
| 6 | **AI=虚拟手柄:产出与键盘完全相同的输入流** | AI 和人走同一输入管线,行为天然合法、测试确定性(种子);换难度只改权重表 |
| 7 | **素材=纸娃娃像素装配,零图片文件** | 先例项目标准;特瑞需要 ~60 个姿势,逐张手绘字符画不现实,按"部件+姿势表"程序拼装,预渲染缓存 |
| 8 | **每帧管线:输入→状态机→判定→结算→物理→回合→渲染** | 阶段顺序固定,模块边界清晰,每阶段可独立测试 |
| 9 | **打击停顿(hitstop)做在 Match 层** | 打击感的来源(命中瞬间双方定住几帧);双方冻结必须同层管理,放任一 Fighter 都不对 |

### 4.2 屏幕与坐标系

- 逻辑画面 320×224,舞台宽 640(比画面宽,摄像机跟随),地面 y=190(角色脚底)
- 开局站位:P1 x=248、P2 x=392(舞台中心 320 ±72),P1 面右、P2 面左
- 摄像机:x = clamp(双方中点 − 160, 0, 320);双方 x 被夹在 [cam_x+8, cam_x+312]
- **判定框坐标系**:相对角色脚底中心、按"面朝右"定义;面朝左时 x 轴镜像(x' = −x2 ↔ −x1)。受击框:站 30×92、蹲 30×58;攻击框逐招在 JSON 里;**空中无身体推挤框**(KOF 特色:跳跃可以换边,落地按相对位置刷新朝向);地面推挤框:站 32×92、蹲 32×58(横向推开用)

### 4.3 角色系统(状态机 + 帧表)

状态清单(每状态一句话规则):

| 状态族 | 状态 | 规则 |
|---|---|---|
| 基础 | idle / walk_fwd / walk_back / crouch | 可随时出招;蹲时轻点即出蹲姿攻击 |
| 移动 | prejump(4f) / air(跳) / land / run / run_stop / dash_back | prejump 内松开上=小跳;run 双击前方向后按住持续跑;后撤=固定弧线小跳22帧 |
| 防御 | block_stand / block_crouch | 按住后方向进入;承招后进 blockstun(数据表逐招),被推背 |
| 攻击 | attack(move_id) | 通用单状态+逐招数据:startup(出招无判定)→active(判定框生效)→recovery(收招不可动);cancel 窗口按招表 |
| 硬直 | hit_stand / hit_crouch / blockstun | 受击/防御期间输入一律无效(缓冲除外) |
| 空中受击 | hit_air / fall / knockdown / wakeup | 被打浮空可被追打(伤害×60%),落地进 knockdown;起身 24 帧、前 8 帧无敌 |
| 投技 | throw_whiff / throw_grab / thrown | 投技有失败动作(12帧);被抓者进 thrown 走脚本轨迹 |
| 表演 | win / lose / intro | 回合结束演出 |

- **小跳/大跳**:prejump 4 帧内松开↑ → 小跳(vy0=+4.0,y 向上为正);否则大跳(+8.0);重力 −0.35/帧²;前跳 vx=+2.8(小跳+1.8)、后跳 vx=−2.4;空攻命中或被防后落地 lag 2 帧,空跳落地 0 帧
- **取消链**:普通技 → 必杀技 → 超必杀(逐招 cancels 表控制);投技不可取消
- **伤害缩放**:连段内第 n 击伤害 ×[100,90,80,70,60,50]%,50% 保底(第 6 击起)

### 4.4 判定与结算

每帧判定顺序(由 `combat.py` 统一裁决):

1. 双方 ActiveAttack(招式+判定框+位置)互验:框相交才可能命中
2. **guard 层级**:mid(站蹲都可防)/ low(只能蹲防)/ high(只能站防——跳攻击、爆裂踢);投技 unblockable(例外:受害帧处于 hitstun/空中不可投)
3. 同帧双方 active 都相交 = **互撞(trade)**,各吃一击
4. 一次攻击只命中一次(命中标记);Power Dunk 例外,两段各算一次
5. 结算输出 `HitResult`:伤害(缩放后)、hitstop(命中11/防御9,必杀13/10,超杀20/14;飞行道具:受害8/攻方0)、受击硬直/防御硬直、推背(命中/防御各一档)、吹飞/倒地、双方涨气
6. **角落规则**:受害方已贴墙 → 攻击方自己被推等量;双方永远不被推出舞台
7. 投技路径单独:距离 ≤28px、同帧按住前/后+C 或 D、对手非硬直非浮空 → 抓取(失败播 whiff)

### 4.5 输入系统

- **表示**:每帧一个 `Tick{dirs(绝对方向持有集), pressed(本帧按下), released(本帧抬起)}`;键盘和 AI 都产出 Tick(同一接口)
- **相对化**:按 facing 把绝对方向换算成 前/后/上/下 再进识别器——镜像问题在此一次解决
- **缓冲**:方向变化历史保留 = 各识别窗的最大值(当前 40 帧,由双 QCF 窗决定;只留 24 帧会识别不出拉长的超杀——A2 交付时发现);按钮按下事件保留 10 帧(搓完指令稍后按拳有效,社区共识缓冲窗,且只认"先搓后按"的先后)
- **指令识别**(滑动窗子序列匹配,**长指令优先**):
  - QCF `↓↘→`:窗口 24 帧,2→(3 可缺)→6,6 与按钮帧差 ≤10;QCB `↓↙←` 同理
  - DP `→↓↘`:窗口 18 帧,6→2→3+按钮(Power Dunk 用)
  - 蓄力:`↓` 连续持有 ≥40 帧且期间无 `↑`,之后 4 帧内 `↑`+按钮 → 升龙撞
  - 双QCF(超必杀):窗口 40 帧,识别优先于单QCF;**气不满不触发**(放行普通 QCF)
- **前冲**:6 松开后 8 帧内再按下 → 进 run(按住持续,松开 6 帧制动);双击 4 → dash_back 固定弧线
- 投技、防御、普通技直接由 Tick + 站姿/距离判,不走缓冲识别
- pygame 侧只用 KEYDOWN/KEYUP 事件维护状态(事件边沿),**不用 key.get_pressed 参与招式判断**(防系统连发干扰),移动读持有集

### 4.6 AI(入门档)

- `TerryAI(ai.json, seed)` 每帧被喂 `AiView`(见契约),每 6 帧决策一次(反应延迟),产出合法 Tick
- 距离三档:远 >160px / 中 90~160 / 近 <90;血量低或己方贴角时换保守权重档
- 关键反应:对手在近距离出招(startup 期)→ 55% 概率转防御;对手跳入(距离<120)→ 75% 概率升龙撞迎击;气满且近距 → 10% 概率超杀
- 权重全部在 `ai.json`(附录 C),种子固定 → **决策序列可复现**(测试和 demo 依赖这个)
- AI 与人共用输入管线,只会产出"合法操作"

### 4.7 素材(纸娃娃像素装配)

- `art/terry.py`:身体部件像素图(头+红帽、金发、红马甲躯干、手臂×姿势、腿×姿势、拳套、鞋——每个是小字符画)+ 姿势装配表(app: 護|`POSE 注册表`(附录 D,~60 键:每键=部件摆放+偏移)拼成整帧 Surface,按 (键,帧,朝向,配色) 预渲染缓存
- 配色:P1 红马甲经典版 / P2 蓝马甲换色版;皮肤/金发/红帽两版共用
- `art/stage.py`:美国街头黄昏背景——天空色带、楼群剪影(视差两层)、铁丝网、地面;`art/fx.py`:能量波弹体、喷泉爆发、命中火花、防御火花
- 动画与帧数解耦:招式计时由数据表驱动,姿势按阶段映射(startup→预备姿、active→发力姿、recovery→收势),跳攻击两姿循环
- **验收一律像素采样**(get_at 颜色/占比断言)+ `scripts/screenshot.py` 无头截图非空统计;**红线:任何 PNG 不 Read 进会话**

### 4.8 HUD / 场景流程 / Debug

- HUD(KOF98 风格):顶部双体力条向中间消耗、中间倒计时、名字;底部能量槽(满档 MAX 变色);回合胜点亮灯;连段计数("N HITS")受击方侧弹出
- 场景栈(照 sheepandsheep):Title(按任意键)→ RoundIntro(ROUND N / FIGHT! 各 ~60 帧)→ Fight → KO 演出(慢动作 + K.O. 大字,满血赢加 PERFECT)→ MatchEnd(胜负画面)→ 回 Title;Esc 暂停覆盖层(定格底图技巧照 sheepandsheep ResultScene)
- 三局两胜:双 KO 双方各记一局;超时血多者胜、同血平局各记一局;3 平局少见路径按 KOF 惯例再来一局(先赢满 2 局者胜)
- F1 切换判定框显示(红=攻击框、黄=受击框、绿=推挤框)——调参必备;`game/main.py` 照先例双跑法样板

### 4.9 风险点与对策

| 风险 | 对策 |
|---|---|
| 帧数全是近似值,手感可能差 | 全部数值进 JSON,batch D 专门一轮试玩调参(改数值走文档变更流程) |
| 纸娃娃拼出来的角色呆/丑 | 关键姿势表先出 3~4 张无头截图给用户过目(A5 交付节点),不行当场改;配色检索已核实 |
| 判定框数值靠估 | 数据表独立于画面 + F1 可视化调框 |
| 输入缓冲边缘情况(连按/斜角丢失) | 识别器专项测试(第 6 节),宽容规则参数化 |
| AI 太强/太弱 | 权重表数据化 + demo 自对弈观察;入门档目标是"能玩",不是"会玩" |
| 搜索工具本次不稳/权威数据被墙 | 已如实标注;调参以试玩为准(真机说了算) |
| 高刷屏下 tick 节奏 | `clock.tick(60)` 锁帧 + 逻辑与显示同帧,不涉及 dt 漂移 |

---

## 5. 模块划分与接口契约(大规模并行开发的分工依据)

### 5.0 目录树(batch 0 建立)

```
~/kof98/
  拳皇98复刻-设计文档.md      README.md
  run.sh  test.sh  .gitignore(.venv/ __pycache__/ *.pyc .pytest_cache/)
  game/
    __init__.py  main.py            # 双跑法样板,拉起 App
    core/                            # ★零 pygame import 的纯逻辑,全部可独立单测
      __init__.py  types.py          # 冻结契约:公共数据类型(下 5.1)
      data.py                        # JSON loader + 校验(DataError)
      motion.py                      # 输入缓冲+指令识别+蓄力+前冲
      fighter.py                     # 角色状态机+出招/取消/投技/受击
      physics.py                     # 重力/速度/推背/墙角(纯函数)
      combat.py                      # 判定框相交+命中结算+连段计数与缩放
      projectile.py                  # 能量波实体
      ai.py                          # AI 决策(虚拟手柄)
      match.py                       # 对局编排:每帧管线+回合/摄像机/打击停顿
    poses.py                         # POSE 注册表(纯数据,键名冻结;core/assets 共用,零 pygame)
    assets.py                        # 字体/配色/纸娃娃精灵装配与缓存(A5)
    art/  terry.py  stage.py  fx.py  # 纸娃娃部件+姿势装配/背景/特效
    ui.py                            # 主循环+场景栈+HUD+键盘→Tick+渲染合成
  game/data/  terry.json  system.json  ai.json
  tests/  test_data.py test_motion.py test_fighter.py test_physics.py
          test_combat.py test_projectile.py test_ai.py test_match.py
          test_art.py test_ui.py test_integration.py
  scripts/  screenshot.py  demo.py   # 无头截图验收 / AI自对弈整局验收
  .venv/ (gitignore)
```

### 5.1 冻结契约(batch 0 写进 `core/types.py`,全组只许 import、禁止改动)

```python
Dir    # 绝对方向枚举: L R U D(斜角=组合持有,不单列)
Btn    # 按钮枚举: A B C D
Side   # P1 P2

Tick(dirs: frozenset[Dir], pressed: frozenset[Btn], released: frozenset[Btn])  # 每帧输入

FrameTriggers(specials: tuple[str, ...],   # 本帧触发的必杀/超杀 move_id(可能空)
              dash_fwd: bool, dash_back: bool)

Box(x1, y1, x2, y2)      # 相对脚底中心、面朝右;x1<x2, y1<y2;overlaps(other) -> bool

Guard   # MID(站蹲可防) LOW(仅蹲防) HIGH(仅站防) UNBLOCKABLE(投技)
Knockdown  # NONE AIR_JUGGLE(浮空追打) SWEEP(扫倒) HARD(重击倒地)
Cancel  # SPECIAL SUPER

MoveDef(name, kind,          # NORMAL SPECIAL SUPER THROW
        startup, active, recovery,          # 帧
        damage, chip, hitstun, blockstun,   # 伤害/削血/硬直帧
        pushback_hit, pushback_block,       # 像素/帧
        guard, knockdown, cancels: list[Cancel], gauge_gain,
        input,                 # 普通技: 按钮+站姿;必杀: 指令模式;投: 方向+按钮
        boxes: dict[帧号 or 阶段, AttackFrame])
AttackFrame(hurt: list[Box], hit: list[Box])   # 该帧受击框覆盖/攻击框

ActiveAttack(move: MoveDef, phase_frame: int, world_hitboxes: list[Box], attacker: Side)

HitKind  # HIT BLOCK TRADE WHIFF THROW
HitResult(victim: Side, kind: HitKind, damage: int, hitstop: int,
          stun: int, pushback: float, knockdown: Knockdown,
          gauge_attacker: int, gauge_victim: int, juggle_bounce: float)

FighterView(pos, state, guarding: Guard 情况, airborne, in_hitstun, boxes, invuln_frames)  # 只读视图
AiView(dist, own_health, own_gauge_full, opp_airborne, opp_attacking, opp_projectile, self_cornered)  # AI 只看得到这些
FighterSnapshot(state, move_id, phase, anim_pose, x, y, facing, health, gauge, combo, palette)  # 渲染只看这个
```

- 错误处理纪律:loader 校验失败 → `DataError("terry.json:power_wave.startup=…")` 带字段路径,不许静默;模块间不许吞异常;UI 层只捕获 DataError 转屏幕报错

### 5.2 模块清单与地盘(并行组 A,五模块文件互不重叠)

| 模块 | 职责一句话 | 只许动这些文件 | 依赖 |
|---|---|---|---|
| A1 角色 | 状态机全状态+出招/取消/投技/受击/浮空/起身 | `core/fighter.py`, `core/physics.py`, `tests/test_fighter.py`, `tests/test_physics.py` | types, data(JSON 只读) |
| A2 输入 | 缓冲/指令/蓄力/双击前冲/镜像 | `core/motion.py`, `tests/test_motion.py` | types |
| A3 判定 | 相交/guard/trade/结算/缩放/能量波 | `core/combat.py`, `core/projectile.py`, `tests/test_combat.py`, `tests/test_projectile.py` | types |
| A4 AI | 距离档决策+反应延迟,产 Tick | `core/ai.py`, `game/data/ai.json`, `tests/test_ai.py` | types |
| A5 素材 | 特瑞全姿势/背景/特效像素装配(HUD 由 batch B 的 ui 直接画,不占 A5) | `assets.py`, `art/*.py`, `poses.py`(只许改帧姿势数), `tests/test_art.py` | types(只用 POSE 键名) |

- **组 A 纪律**:禁止改 `types.py`(契约有问题→回报主会话统一改);**禁止改 `game/data/terry.json`、`system.json` 数值**(发现数值不合理→报告,走文档变更改基线和测试)
- **依赖方向**(无环):`types ← {data, motion, fighter, physics, combat, projectile, ai, match} ;fighter/physics/motion/combat/projectile/ai → data ;match → 全部 ;ui → match, assets`

### 5.3 各模块接口契约(输入/输出/验收)

- **A2 motion**:`MotionInput().feed(tick, facing) -> FrameTriggers`;`charge_ready(dir) -> bool`(蓄力查询);内部维护缓冲。验收:附录 A 全部指令的识别用例(含镜像、宽容、缓冲超时、蓄力、长指令优先)
- **A1 fighter**:`Fighter(cfg, moves, side)`;`step(triggers, tick, opp_view) -> None`(状态推进+产生 ActiveAttack,每帧按对手位置刷新朝向);`active_attack() -> ActiveAttack|None`;`apply_hit(hit: HitResult) -> None`(受方:扣血/硬直/倒地/浮空/被推/涨气);`on_attack_landed(hit: HitResult) -> None`(攻方:combo++、涨气、标记当前判定窗已用);`snapshot() -> FighterSnapshot`;`can_be_thrown() -> bool`。combo 在自己观察到对手脱离受击态时清零。验收:逐帧断言 st.C 的 3/3/16 阶段、硬直内输入无效、取消窗、小跳大跳轨迹、投技距离与 whiff、受击状态族
- **A1 physics**:`step_gravity(vx, vy) -> (vx, vy)`、`apply_pushback(f, wall_x) -> (f, x)`(墙角攻方回推)、`jump_arc(kind) -> (vx0, vy0)` 纯函数。验收:跳高/滞空帧数精确断言、推背不穿墙
- **A3 combat**:`resolve(atk: ActiveAttack, victim: FighterView, victim_boxes) -> HitResult|None`;`resolve_trade(a, b) -> (HitResult, HitResult)|None`;`ComboCounter.hit(damage) -> int`(返回缩放后伤害);`Projectile(spawn_x, facing, speed, move).step() / boxes() / alive`。验收:第 6 节全部相交/防御层级/互撞/缩放/同屏一发用例
- **A4 ai**:`TerryAI(cfg, moves, system).next_tick(view: AiView) -> Tick`(种子走 cfg.seed)。验收:同种子同序列(跑两遍逐 Tick 比对)、距离档行为分布(1000 种子统计落入容差)、反应延迟 6 帧内不换决策、只产合法 Tick
- **A5 assets/art**:`assets.py: load_palette(side) / sprite(key, frame, facing) -> Surface(缓存) / POSE_KEYS 注册表(键名冻结=附录 D,帧姿势数可在此定)`;`art/terry.py` 供部件。验收:全部键非空+关键像素颜色断言(帽红/裤蓝/两版马甲换色)+ 左右翻转镜像对称断言
- **batch B(主会话)match**:`Match(cfg, moves, ai_cfg, seed).step(tick_p1, tick_p2) -> MatchSnapshot`(内部走 4.4 管线+摄像机+hitstop+回合);`MatchSnapshot` 供渲染。验收:KO/超时/双 KO/三局两胜流程测试

### 5.4 并行节奏

batch 0(串行,契约冻结)→ **组 A 五模块并行**(互不接触对方文件;各自只跑自己的测试文件)→ batch B(串行,主会话集成)→ batch C(串行,集成验收+首推)→ batch D(串行,试玩调参+交付)。集成验收单列一个 batch,专门验"接口两侧真的对得上"。

---

## 6. 测试计划(考卷——写完请你审,审过不许 AI 私改)

> headless 套路:涉 pygame 的测试文件顶部先设 `SDL_VIDEODRIVER=dummy`(+AUDIO)再 import;数值断言一律对着 JSON 加载值或附录基线;关键公式测试内独立复算(照 sheepandsheep 惯例)。

**test_data.py(loader 与校验)**
- 正常:三份 JSON 全量加载,字段齐全类型对
- 异常:startup=−1、damage=0、box 的 x2≤x1、缺字段、active=0(打击技)→ 各自 `DataError` 且报出"文件名.招名.字段"
- 完整性:非投技招每个至少 1 个 active 攻击框;每个 POSE 键(附录 D)都有注册
- 复算:任取 3 招,测试内独立算 startup+active+recovery == 总帧数

**test_motion.py(指令识别,喂合成 Tick 流)**
- `↓ ↘ → +C` 逐帧 → 恰好触发 power_wave 一次
- 宽容:`↓ → +C`(缺 ↘)→ 仍触发;`↓ (间隔超过 24 帧)→ +C` 超窗 → 不触发(勘误:原写"停20帧",span≈21<24 实际仍在窗内,该用例本身是错的,已改为无歧义的超窗间隔——考卷数值修正,向用户备案)
- 缓冲:搓完 9 帧后按 C → 触发;11 帧后 → 不触发(缓冲=10)
- 蓄力:↓ 持有 40 帧后 ↑+C → rising_tackle;持有 39 帧 → 不触发(阈值 40);中途断开重计
- DP:6→2→3+P → power_dunk;超 18 帧窗 → 不触发
- 超必杀:双 QCF+O 且气满(外部传入)→ power_geyser;**长指令优先**:同一结尾的单 QCF 识别让位
- 镜像:同一物理输入流,facing=−1 时识别出的指令是同一招(相对化正确)
- 前冲:6 松开 8 帧内再按 → dash_fwd=true;间隔 9 帧 → false;双击 4 → dash_back

**test_fighter.py(状态机逐帧)**
- st_C 按下:帧 0~2 无攻击框(startup=3)、帧 3~5 有(active=3)、帧 6 起无;总帧 3+3+16 后回 idle
- hitstun=17 期间:输入任何东西状态不变,缓冲里的招在硬直结束帧按序触发
- 取消:st_C active/recovery 前 8 帧内触发 power_wave → 切换成立,cancels 表外的 st_D → special 不成立
- 小跳:prejump 第 3 帧松↑ → 轨迹最高点 ≤25px;按住 → ~91px±2;滞空帧数精确断言
- 投技:距 28px 内 前+C → 对手进 thrown;29px → whiff 播 12 帧不抓人;对手在 hitstun → 不可投
- 浮空连段:空中被打 → hit_air+弹起;再命中 → 伤害×60%;落地 → knockdown;起身前 8 帧无敌(框内攻击 whiff)
- 防御:站防挡 mid/high、蹲防挡 mid/low、站防不挡 low(扫堂腿命中)、蹲防不挡 high(跳攻击命中)
- 能量:命中+8/+4、被击+5、防御+2、超杀扣 100、不满 100 拒发

**test_physics.py**
- 重力/跳弧数值断言(独立复算公式);墙角推背:受害贴墙时攻方位移=推背量、否则受害方退;双方不越舞台界

**test_combat.py / test_projectile.py**
- 相交→命中、相离→whiff、同帧双 active 相交→trade 各吃一击、一次攻击只结算一次
- 全 guard 层级矩阵;chip 只在防御时扣且必杀才有;缩放序列 100/90/80/70/60/50/50…
- hitstop 帧冻结语义(计数期不推进)——在 match 层验证,这里只验 HitResult 数值
- 能量波:前进 3.5px/帧(A 版)、命中后消失、出界消失、同屏 1 发(前弹未灭时再搓不产新弹)

**test_ai.py**
- 固定 seed=42:两遍跑逐 Tick 完全一致
- 反应延迟:对手出招后 6 帧内决策序列不变
- 分布:远档 1000 个种子跑 10 决策,接近类动作占比 45%±8(容差档)
- 合法性:只产合法 Tick;不会凭空出现人类按不出的操作

**test_match.py / test_roundkeeper(并入 match)**
- 完整三局两胜:2:0、2:1、双 KO 各记一局、超时血多者胜、同血平局
- 摄像机:中点跟随+clamp;角色不越 [cam+8, cam+312]
- hitstop:命中后全局冻结 11 帧再恢复推进

**test_art.py(像素采样)**
- 附录 D 全部键非空;帽子区域红色占比 >40%;牛仔裤蓝;P1/P2 马甲红/蓝互换断言
- facing 翻转:像素级左右镜像断言
- 能量波/喷泉/火花帧非空

**test_ui.py(无头)**
- App.step 单帧驱动:注入 KEYDOWN WASD/UIOP → Tick 正确;失焦事件 → 暂停态
- 场景栈流转 Title→RoundIntro→Fight→KO→MatchEnd→Title
- HUD 像素采样:满血/半血管像素数、MAX 变色、倒计时数字非空
- dummy 下 SCALED 自动剥离(照 mota50)

**test_integration.py(端到端,headless)**
- 脚本化人类输入流(含一记 QCF+O)打 AI,60 秒内出现 KO 或超时,无异常
- demo:种子 42 AI 自对弈整场(≤3 回合),退出码 0;血量单调不增(无负伤害/回血 bug)
- F1 调试开关:开启帧渲染含红黄绿框像素

**跑法**:`./test.sh`(= cd 仓库根 + unset PYTHONPATH + `.venv/bin/python -m pytest tests/ -v`);预期:全绿;组 A 期间各模块只跑自己文件

---

## 7. Batch 划分与进度

| # | 目标(做完后系统什么状态) | 涉及文件 | 验证方式 | 并行组 | 状态 |
|---|---|---|---|---|---|
| 0 | 骨架+契约冻结:仓库/venv/脚本/game骨架/types.py 全部契约/三份 JSON 基线/loader+校验/POSE 注册 | 除 art/ai/motion/fighter/combat/match/ui 外全部 | pytest(test_data)绿 + run.sh 拉起空窗 + 首次 commit(init:) | — | ✅ |
| A1 | 角色状态机+物理:Fighter 全状态可逐帧驱动 | core/fighter.py, physics.py + 测试 | test_fighter/test_physics 绿 | A | ✅ |
| A2 | 输入系统:合成 Tick 流能触发全部指令 | core/motion.py + 测试 | test_motion 绿 | A | ✅ |
| A3 | 判定结算:相交/防御/互撞/缩放/能量波 | core/combat.py, projectile.py + 测试 | test_combat/test_projectile 绿 | A | ✅ |
| A4 | AI:种子确定的决策器 | core/ai.py, ai.json + 测试 | test_ai 绿 | A | ✅ |
| A5 | 素材:~60 姿势+背景+特效,像素采样过 | assets.py, art/* + 测试 | test_art 绿 + 3~4 张无头截图**给用户过目** | A | ✅(截图待用户过目) |
| B | 集成:对局管线+场景栈+HUD+键盘,能完整打一场(无头) | core/match.py, ui.py, main.py + 测试 | 全量 pytest 绿 + 无头跑一场无异常 | — | ✅ |
| C | 集成验收:自对弈整局+截图+全量回归,**首推 GitHub** | scripts/demo.py, test_integration.py | demo 退出码 0 + 全绿 + push 成功 | — | ✅ |
| D | 调参交付:试玩反馈一轮、数值调优(走文档变更)、README、清单勾完、终推 | data/*.json、README、本文档 | 你把第 9 节清单全部勾完 | —(含你) | 待执行(素材 v2 后收尾) |
| E | 素材v2-管线:真贴图导入(sheet 拆格/manifest/P2换色/回退)+ 出招 timing 对齐(打击帧=判定帧) | game/sprites.py, assets.py, ui.py, scripts/sprite_check.py + 测试 | test_sprites/test_anim 绿 + 全量回归 + 无真图时游戏照常跑(回退) | — | ✅ |
| F | 素材v2-程序绘制加帧:姿势家族拆模块并行补中割帧(走 Workflow 多代理) | art/terry_*.py 家族模块, poses.py, terry.py 门面 | test_art 更新绿 + 像素采样验收 + 全量回归 | F 内并行 | ✅ |
| F2 | 观感补丁:消化 §10.12 清单(踢腿方向/纵移表达/蹲脚贴地/腰缝/剪影分化/开场定格) | terry_base.py, terry_specials.py, terry_attacks.py, terry_reactions.py, ui.py | fam_check 四家族 + 全量回归 + 截图刷新 | F2 内并行 | ✅ |
| G | 素材v2-导入实测:真 Terry sheet 下载放置 → 配 manifest → sprite_check 全绿 → 视觉过目 → 打击感调参 | game/sprites/terry/(gitignore), scripts/sprite_check.py, tests/conftest.py | sprite_check 退出码 0 + 全量回归 + demo 整局退出码 0 + 你过目预览图/截图 | —(含你) | ✅(截图待用户过目) |

说明:每 batch 完成即本地 commit(信息带测试数);组 A 五个模块由五个子代理并行,文件互不重叠、契约已冻结,各模块交付后主会话单独验收再合并。

---

## 8. 执行记录

| 日期 | Batch | 结果 | 备注(Commit/问题) |
|---|---|---|---|
| 2026-09-10 | 文档 | ✅ 定稿,用户已审 | commit 5dca695;supercombo wiki 被 Cloudflare 403,帧数全部采用近似基线(标注推测待验证) |
| 2026-09-10 | 0 | ✅ 32 测试绿 + 冒烟通过 | commit c12b044;venv 因系统缺 ensurepip,改用 `--without-pip` + 复制 mota50 的 site-packages(pygame 2.6.1 / pytest 9.1.1,与先例项目环境一致,零网络) |
| 2026-09-10 | A3 | ✅ 43 测试绿 | commit 96507ea;契约裁决 6-10 条(301d41b)同步全组 |
| 2026-09-10 | A1/A2 | 🔄 重启 | 两个子代理先后因单次输出超 32K 上限被掐断(未写出文件),已唤醒并要求分块写作 |
| 2026-09-10 | A5 | ✅ 24 测试绿,全量 99 绿 | commit e611770(素材)+ f8933a2(截图脚本);34 部件 108 帧;poses.py 未动;截图在 scripts/out/ 待用户过目 |
| 2026-09-10 | A4 | ✅ 12 测试绿,全量 125 绿 | commit 9df029a;A4 提的 4 条疑点已裁决:§5.3 签名按实际契约修订、附录C保守档与 ai.json 对齐、reactions.block_dist(现为常量100)进 batch D 调参旋钮清单、自补的超杀反应测试属加严予以保留 |
| 2026-09-10 | A1 | ✅ 55 测试绿 | commit 3d9cafa + 契约补丁 b9428e2;七条自由裁量全部批准;发波招增 projectile.start 字段(A=14/C=18) |
| 2026-09-10 | A2 | ✅ 24 测试绿,全量 190 绿 | commit batchA2;六条疑点裁决入档(含 §6 一处考卷数值勘误:停20帧 span≈21<24 原用例数学上不成立,改为无歧义超窗断言);**组 A 五模块全部收官** |
| 2026-09-10 | B | ✅ 26 测试绿(match 16 + ui 10),全量 216 绿;无头整场冒烟:乱按玩家 vs AI 3810 帧打满三局无异常(P2 2:0 胜,暂停/F1/KO 慢动作全链路) | match 层 commit 0247506、ui 层 commit 见日志;契约裁决 17-19 条入档 |
| 2026-09-10 | C | ✅ test_integration 2 条绿(脚本人类含 QCF+C 全链路打满一回合、demo 子进程双跑退出码 0 且逐字节一致),全量 218 绿;demo:种子 42/7 自对弈 5248 帧 3 回合 P1 2:1,关键帧截图 6 张在 scripts/out/;**首推 GitHub** | demo 修 KO 截图逐帧刷屏(改每回合一张);考卷"QCF+O"按附录E键位(O=重拳C)实现 |
| 2026-09-10 | E | ✅ test_sprites 17 + test_anim 6 绿,全量 244 绿;sprite_check 无图路径退出码 0;demo 复跑退出码 0 | 素材 v2 需求(真实贴图+动作加帧)三问已确认方案入档 §11;真图目录 gitignore,合成图自测零版权依赖;发现并修正 ui 取帧反向 bug(判定帧显示起手姿势) |
| 2026-09-10 | F(进行中) | 拆分✓:terry 按家族拆 base+locomotion/attacks/specials/reactions+门面(commit 31b6c28),拆分前后 41 姿势×96 帧像素哈希逐字节一致,244 绿 | 目标帧数表先行写入 poses.py(96→115 帧,walk 6/jump 3/hit_high 3/st_A 3 等 19 帧位);新考题 test_inbetween_frames_differ(相邻帧≥12px 差掐复制粘贴帧,阈值标定自全表最小合法差 fall=26);家族自检脚本 scripts/fam_check.py(四代理并行时各自的模块级验收门);ASCII 过目工具 scripts/ascii_pose.py(PNG 不进会话红线的配套);四家族 Workflow 加帧进行中 |
| 2026-09-10 | F | ✅ 全量 245 绿(244+新考题);Workflow 9 代理(4 画师+3 对抗验收+2 返修)交付 19 新帧:walk 6 帧真左右交替步态环、jump/jump_fall/dash_back/intro 各 3 帧、受击族 11 姿势全升级、st_A/st_B/cr_A/cr_B/cr_C 补收招帧 | locomotion/reactions 首轮验收被打回(walk 步态角色倒置、air_hit/dash_back/intro 悬空断臂),返修后主会话逐帧 ASCII 复核通过;attacks 一次过;specials 核验零改动(发现 power_geyser 帧1 拳套被躯干遮挡,主会话修画序);新增姿势帧龄锚定(ui 本地,受击/起身/开场演出从第 0 帧按序播,KO 慢动作随慢速走,demo 确定性路径不受影响)+ ANIM_RATE 对齐硬直时长(躺 6/受高 5/受低 8/起身 10) |
| 2026-09-10 | F2 | ✅ §10.12 ①~⑧ 全消化;Workflow 6 代理(3 修缮+3 对抗验收,零返修)一次过 | specials:crack_shoot 前抡弧线(新部件 leg_kick_fwd/fup)、rising_tackle 逐帧升空 28px、power_dunk 帧4 新部件 arm_plunge 拳套压地;base:_crouch lg_dy 76→78 蹲族贴地(全局,四家族复检)+ leg_knee 抬脚鞋补画;attacks/reactions:st_B 新部件 atk_snapkick 与 st_D 分化(剪影差 404→614px)、win 腰缝封缝;主会话补 intro 定格(ANIM_RATE 40)+ land 贴地跟修 + arm_plunge 加长贴地(验收转交项);新增 README.md;两处作者汇报数字不实被对抗验收当场抓出(改前基线张冠李戴/伸及坐标错),代码无恙 |
| 2026-09-10 | G | ✅ FF1 Neo Geo 原版 Terry sheet(SpriteDB,552×2588,scale=1)导入:54 唯一帧 → 32 姿势 75 帧引用,swap_p2 五对(FF1 红系→蓝系);sprite_check 真图 32/回退 9;全量 245 绿(新增 tests/conftest.py 考场环境契约);demo 真图路径整局 5248 帧退出码 0;预览图/场景图/自对弈 6 截图在 scripts/out/ 待用户过目 | 认图全靠 ASCII 工具链(sheet_ascii/frame_probe/frame_cascii,PNG 零进会话);发现考场环境坑:test_art 经 assets.sprite 断言程序纸娃娃,装上真图后 10 题红——conftest 把默认注册表钉死回退态修复(断言零改动,真图路径由 test_sprites 注入注册表覆盖);版权分工变更入档 §11.1 |
| 2026-09-10 | G 修复轮 | ✅ 用户试玩反馈两条(①运动时回退纸娃娃 ②四必杀帧"看着很不相关")全部修复;真图 32→35 姿势、回退 9→6;245 绿 + demo 复跑退出码 0(种子确定性,5248 帧 P1 2:1)+ P2 换色精确色值抽验九帧零漏红 | 核心勘误(色标字符画逐帧重读 + 像素diff 定量复核):**带2三帧=FF1 走路循环**(原误判出拳;对站姿下半区差 50-90% 且无伸臂拳套,而真站姿拳 b3f3 下半区仅差 11.5%=原地挥臂)→ walk/run 上真图;**拳套/鞋边小橙点是配色镶边不是火焰**(真火=成片 Y/W/o 旋涡),由此带3=普通直拳族、带7突进=重拳;**四必杀重新认帧**:能量波=b11f0 聚气+b8f4 拍地地波+b3f0 随势、燃烧拳=b4f2/b13f0 蓄火+b8f2 大火焰突进+b13f1 随势、爆裂踢=b12f2 蓄+b12f1 抬踢+b16f3 翻转(顶=红鞋+牛仔蓝抬腿,原误配 Dunk)+b14f4 落地、升龙撞=b8f5 垂拳蓄火+b17f3 白拖尾升空+b14f1 高点+b11f2 下落、Power Dunk=b11f3 蓄+b14f2 斜升+b13f2 过顶+b16f4 拳领下砸+b16f5 砸地迸火;**超杀 power_geyser 上真图**(b8f3 拍地拖尾+b17f2 火焰涡爆发,原回退纸娃娃);三对重复帧(b7f0=b7f3/b4f1=b12f0/b8f5=b17f4)0 像素差确认;附带修:st_C 收招帧 b3f0→b3f3(原借的能量波发招帧)、prejump/land 由带火 bend 帧改蹲帧、j_C 增空中下砸拳帧、st_A 改跨步直拳;锚点规则升级=地面帧底部 28% 质心+腾空帧全身质心(脚本算,免手估抖动) |
| 2026-09-10 | G 修复轮2 | ✅ 用户试玩反馈三条(①双击前/后(跑步/后撤)贴图错 ②被打时贴图不对 ③U 轻拳/I 轻脚帧错)全部修复;真图 35→36 姿势、回退 6→5(dash_back 上真图);245 绿 + demo 复跑退出码 0(种子确定性,5248 帧 P1 2:1)+ P2 换色精确色值抽验 17 帧零漏红(含全部新帧) | **流程升级:本轮认图走 Workflow 多代理对抗验收(4 认图员+3 对抗验收员,identify 提案×refuter 反驳,像素级头位/连通域测量裁决)——抓出修复轮我两处确认偏误**:①修复轮我曾确认 b17f0/f1 是受击帧,验收员用火焰连通域测量推翻(208px/127px 成片真火=升龙撞蓄火族);②我配的受击前帧全错(b15f2/f3 是换线速度线特效帧、b7f1 是突进拳族护中门站姿)。**真受击族全在带 19**:b19f0 站立后仰深折+汗滴、b15f1 捂腹前折+汗滴、b19f1 横飞、b19f2 翻滚、b19f3 仰倒带灰尘云、b19f4 平躺→hit_high/hit_low/air_hit/fall/knockdown/wakeup/thrown/lose 整族重配;③**b4f1 压根不是踢腿**(帧底两鞋平插地、两条"小腿"实为缠浅蓝绑带的小臂、顶部"鞋"=帽+脸——举双臂必杀起手式,FF1 特瑞小臂缠布色类同牛仔蓝是伪装根源),故 st_B/j_B/jump_fall 换真踢帧 b12f1(全表唯一站姿真踢,靴柱延伸 dx+42..+50);轻拳 b6f1 提案被头位数据推翻(帽沉至 dy-80、脸埋胸口=下蹲中段)→st_A active=b3f3 直拳;④**walk/run 改 ping-pong 六帧** [迈,并,小迈,并,迈,并] 消除原 [迈,并,迈]×2 中 2→3 格"迈步接迈步"跛行跳变;⑤**dash_back 上真图** [蹲→蜷缩团身(b15f1,引擎惯例:地面帧借 y 位移用于空中语境,先例 jump=jumpup)→蹲],3 格×6 拍对 22 帧时长成立;⑥空中攻击重配:j_A/j_C=b12f2 蜷身前拳(腾空帧)、j_B=b12f1、j_D=b16f3 翻转踢(与 cs_flip 共帧);⑦crack_shoot 起手 b12f2→蹲帧(它实为腾空帧,已派 j_A);wakeup 改 [仰倒→蹲] 直观起身链 |

**执行期契约勘误(不动已批设计的行为,统一约定,向用户备案):**
1. 全项目 vy 与 Box 的 y 统一为"向上为正"(附录 A/B 原负号是屏幕坐标语义,已统一;重力相应为 −0.35)
2. `AttackWindow` 增窗级 knockdown 覆盖字段(Power Dunk 第一段不倒地、第二段重击倒地,附录 A 的"两段各55"靠它精确表达)
3. POSE 注册表独立成 `game/poses.py`(避免 core/data 反向依赖 assets 把 pygame 带进纯逻辑层)
4. AI 对空迎击招由升龙撞改为 Power Dunk(蓄力招在反应窗口内出不来,入门 AI 用快速 →↓↘ 指令;附录 C 已同步勘误)
5. 世界坐标 y 统一"向上为正",地面 y=0(渲染层才翻屏幕坐标,stage.ground_y=190 只给 ui 换算用;types.py 注释已同步)

**契约裁决记录(A3 交付后主会话确认,对全组生效):**
6. `HitResult.damage` 防御时承载削血值(原契约"damage=0、削 chip"两个语义只有 damage 一个承载位;如 st_C 被防 damage=0、power_wave_A 被防 damage=8)——apply_hit 一律直接扣该值
7. `Projectile` 构造签名定为 `Projectile(move, x, facing, side, bounds)`(契约原缺 side,HitResult.attacker 需要)
8. 投技 pushback = 40 × dir × 投手 facing(带符号,受方直接位移);投技涨气=攻方 move.gauge_gain(基线 0)、守方 0
9. 缩放取整 =(raw×pct+50)//100;连段**第 6 击**起 50% 保底(附录 B 为准;A3 任务卡"第 7 击"系笔误作废)
10. 受方 invulnerable 时裁决返回 None(whiff);trade 一方无敌退回单向路径;空中互撞照常给浮空弹起(先按 hit 规则算完再标 kind="trade")

**契约裁决记录(A1/A4 交付后主会话确认):**
11. 发波招增 `projectile.start` 字段(A=14 / C=18,即附录 A 帧数表的 startup),loader 校验 0<start<total,match 在该帧生成弹体——A1 交付发现的集成缺口,test_data 同步加严断言
12. A1 的 7 条自由裁量全部批准:取消窗=active 起全程、防御态语义(站姿拉后=后走+guarding)、推背=一次性位移、躺地 12 帧+躺地无敌、投技 whiff 吞该帧按钮、until=land 落地 lag=2、攻击中锁不刷新朝向
13. batch B 集成要点:①墙角时攻方回推量=physics.pushback_resolve 第二返回值,由 match 应用;②发波生成用 Fighter.attack_frame() 对照 projectile.start;③types.py 推背注释勘正为 px(一次性)
14. A4 签名按实际契约为 `TerryAI(cfg, moves, system)`(种子在 cfg.seed),§5.3 已修订;超杀计划(含按钮、8 帧)执行期跳过决策点,防 6 帧节流掐掉按钮帧;reactions.block_dist(现常量 100)列入 batch D 调参旋钮
15. A2 六条疑点裁决:方向历史保留=各识别窗最大值(40);蓄力释放后按按钮窗(现常量 4 帧)列入 batch D 调参旋钮;按钮缓冲只认"先搓后按";§6"停20帧超窗"系考卷笔误已修正(非放宽,原用例数学上不成立);U+C 笔误照 terry.json 按钮映射测;同帧双按钮 tie-break=超杀>必杀>更长指令>字典序(确定性)
16. 组 A 收官:五模块 158 条模块测试(55+24+43+12+24)+batch0 32 条,全量 190 绿,零地盘冲突,零契约私改

**契约裁决记录(batch B,主会话集成期):**
17. 投技集成缺口:A1 的 `_try_throw` 进 THROW_GRAB 时未存 move 引用,match 无法知道是前投还是后投 → fighter 补 `_throw_move` 存储 + `current_throw()` 访问器(最小改动,tests/test_fighter 既有断言不受影响)
18. 窗口呈现:原计划 pygame.SCALED 自动整数放大,但 SCALED 自选倍数(1080p 屏会选 4×=1280×896),与本文档承诺的 960×672 不符 → 固定 `set_mode((960,672))` + `transform.scale` ×3 手动放大;dummy 无头环境同一代码路径零特判(§6 test_ui 的"SCALED 自动剥离"条款按此落地为"dummy 下建窗正常")
19. hitstop 冻结模型:match 用**每方独立计数** `freeze[side]` 落地 §4.4——近身命中/防御双方同冻(=考卷"全局冻结 11 帧"的精确实现),弹体命中只冻受方(§4.4"弹受方 8",攻方照动,自由方可继续连段);投技 hitstop=0;有任何一方冻结的帧回合计时不走
20. P2 对手 AI 种子取 7(与 ai.json 的 42 错开,避免双方镜像自对弈;同种子自对弈已验证为完美镜像 435:435)——裁量,batch D 可调
21. KO 慢动作:round_end 前 20 个对局帧按 3 UI 帧 1 步放慢,其后正常速;回合结束演出共 120 对局帧(≈2.7 秒)——裁量,batch D 可调

---

## 9. 真机验证清单(试玩清单,全部勾完才算交付)

**基础操作**
- [ ] `./run.sh` 拉起 960×672 窗口,标题画面按任意键开局,ROUND 1 → FIGHT! → 开打
- [ ] 前后走、蹲、小跳(轻点)/大跳(按住)、前冲(双击前)、后撤,速度手感正常
**招式全表(附录 E)**
- [ ] 站/蹲 A B C D 共 8 个普通技全部能出、判定段位对(扫堂腿蹲防才挡得住、跳攻击站防才挡得住)
- [ ] 空中 A B C D 四个跳攻击能出,跳入能接地面轻攻击形成连段
- [ ] 能量波 ↓↘→+O(快版)、↓↘→+U(慢版);同屏一发限制
- [ ] 燃烧拳 ↓↙←+拳(突进)、爆裂踢 ↓↙←+脚(弧线跳踢)、Power Dunk →↓↘+脚
- [ ] 升龙撞 ↓蓄↑+拳:蓄够 0.7 秒再↑+拳能出、蓄不够不出
- [ ] 攒满能量(MAX 变色)→ ↓↘→↓↘→+O 出能量喷泉,气槽清空
- [ ] 投技:贴脸按 前+O 抓人摔;距离不够出失败动作
**对局规则**
- [ ] 防御(按住后)生效:掉血变削血、被推背;角落推人不穿墙
- [ ] 被打有打击停顿感和连段计数;浮空能追打;倒地起身有一小段无敌
- [ ] AI 会追会打会挡会跳入,难度像"入门";三局两胜整场能打完,K.O./PERFECT/胜负画面正常
- [ ] 倒计时走完按血量判胜负
**异常路径**
- [ ] 窗口切到后台再切回:自动暂停,回来恢复,不错帧不炸
- [ ] Esc 暂停/恢复;关闭窗口干净退出无报错
- [ ] 乱搓乱按(同时按满键盘)不触发非法状态
**性能与工具**
- [ ] 全程肉眼流畅 60fps;F1 能开/关判定框显示

---

## 10. 遗留风险与未验证点(诚实清单)

1. **帧数表全部为近似基线**(权威源被 Cloudflare 挡)——标注"推测,待验证",以试玩调参为准
2. High Angle Geyser(第二超必杀)指令未核实,第一版砍掉
3. 输入缓冲/蓄力阈值数值(10/24/18/40 帧)是社区共识+设计值,未经在线核实,试玩校准
4. 投技指令采用"方向+C/D"(与 98 一致性属低风险设计选择)
5. 双 KO 记分规则按 KOF 惯例"各记一局"实现,未核实 98 原版细节
6. AI 强度纯靠权重表,入门档是否合适要试玩才知道
7. 纸娃娃像素观感风险,A5 节点用户过目把关
8. 能量槽简化为单条(98 原版为 3 段),ADV/EX 模式整体不做
9. 跳攻击落地/取消细节、贴身换边等角落规则做了合理简化,未逐条对照原版
10. **墙角推背双界相互作用**:apply_hit 的 pushback_resolve 以舞台界(0..640)为界,摄像机随后又把双方夹回可视边([cam+8, cam+312],最右 632)——受方贴可视墙时被"先推过界再夹回",攻方回退量按舞台界计算(略小于理论值)。视觉结果正确(受方不越界、攻方被推开),数值不严格守恒;若试玩觉得墙角压迫感不对,batch D 可把 match 的墙角回推改用可视边界(batch B 集成期发现,test_match::test_attacker_corner_pushback 有断言钉住现行为)
11. HUD 血条无"伤害延迟条"(红条下面那截黄条),扣血即时反映——简化,batch D 可加
12. **素材观感清单**(batch F2 已消化①~⑧:crack_shoot 改前抡弧线、rising_tackle 逐帧升空 28px、power_dunk 帧4 拳套压地、蹲族鞋底贴地 y99、win 腰缝封缝、st_B 新部件 atk_snapkick 前踢与 st_D 分化、leg_knee 抬脚鞋补画、intro 开场定格)。**剩余次要项**(验收员新发现,均 1-6px 级、不挡交付,试玩若在意再修):①st_D 帧1 支撑腿柱 y67-72 有 6 行断口(leg_sidekick 部件自身设计);②leg_crouch_kick 部件小腿末端与鞋之间 1px 缝(cr_B 帧1/cr_D 帧2/power_dunk 帧4 可见);③crack_shoot 帧0 y75 有 1px 透明缝(旧 tuck 帧同款既有现象)
13. **真图导入遗留**(batch G 修复轮2 后,FF1 sheet 覆盖 36 姿势,以下 5 姿势回退程序绘制,真图/纸娃娃混排):stand_block/crouch_block(FF1 无防御姿势)、throw_whiff/throw_grab(FF1 无投技;双人合成帧 b7f2 备案待未来投技)、intro(FF1 无开场演出;片头横幅=带0)。修复轮2 升级上真图的:dash_back(蹲→蜷缩团身→蹲)、受击整族(hit_high/hit_low/air_hit/fall/knockdown/wakeup/thrown/lose,真帧全在带 19)、st_A/st_B(轻拳直拳/轻脚真踢)、j_A~j_D 空中攻击、jump_fall。共帧备案(FF1 无独立帧,语义就近共用):walk/run 同表仅速率区分(FF1 无跑动)、st_B/j_B/爆裂踢踢腿共用 b12f1、j_D/爆裂踢翻转共用 b16f3、j_A/j_C/jump_fall 共用 b12f2、hit_low 主帧 b15f1 兼后撤团身。次要观感(试玩若在意再修):①能量波发招帧(b8f4)内嵌一段地波图,与游戏自身弹体前后衔接——预想为"发招瞬间波从拳下迸出",若觉重复可换 b3f0 纯斜下拳帧;②升龙撞飞升(b17f3)与 Dunk 斜升(b14f2)的朝向/动作语义靠 ASCII 认图推定,若试玩觉得"方向不对"优先复查这两帧;③win=b8f0 身份存疑(更像前倾突进预备帧,非举臂胜利),候选 b18f1 下轮连同 jump/win 一起复核;④j_D 帧底 33px 金色簇(发/火待核)——若试玩觉得空中重踢带火不像普通技再查;⑤b15f0(四肢上举+胸口橙爆,汗/火存疑)未启用,入低置信池待真机

---

## 11. 素材系统 v2(真实贴图 + 动画升级;2026-09-10 用户需求,方案已确认)

### 11.1 需求卡
- 背景:玩家侧反馈两点——①"人物贴图换成真实的" ②"动作僵硬,要按动作设计好每帧"。当前程序纸娃娃 + `af % n` 循环取帧,判定生效帧反而显示起手姿势,是僵硬主因之一。
- 目标:真 KOF98 风格贴图管线(用户自备图)+ 出招动画打击帧严格对齐判定帧 + 程序绘制补中割帧(等图期间立刻见效)。
- 版权边界(2026-09-10 batch G 起按用户指示修订):版权责任**归用户**;用户本机无法开浏览器,检索/下载/放置由我**代劳**(用户原话"版权问题交给我 我现在没法打开浏览器")。两条硬边界不变:①**仓库永不收录版权图**(`game/sprites/` 整目录 gitignore,已用 `git check-ignore -v` 验证命中);②站点用技术手段拦命令行下载的不绕(TSR 全站 Cloudflare 403、SpriteDB POST 被 Mod_Security 406,均改道)。首个素材:SpriteDB 的 **Fatal Fury 1 Neo Geo 街机原版 Terry sheet**(其招式集与本作实现的必杀完全对应)。

### 11.2 方案
**A. 真贴图管线(game/sprites.py,零 pygame 之外的依赖)**
- 目录:`game/sprites/terry/`,内放 `manifest.json` + 若干 PNG sheet(用户自备;TSR 等站下载或自己提取)。
- manifest 格式(键名冻结):
```json
{
  "scale": 1,
  "swap_p2": [[[196,32,32],[44,88,196]]],
  "poses": {
    "idle": {"sheet": "idle.png", "strip": {"count":4,"fw":56,"fh":100,"ax":28,"ay":100}},
    "st_C": {"sheet": "attacks.png", "frames":[{"x":0,"y":0,"w":60,"h":98,"ax":30,"ay":98}]}
  }
}
```
  * `strip` = 均匀网格切格(左→右,可加 `"rows":n`);`frames` = 逐帧显式矩形(不规则排版用);
  * `ax/ay` = 帧内**脚底中心**锚点;返回 Surface 统一衬底到"脚底=画布底、水平居中",ui 落点零改动;
  * `scale`:sheet 放大倍数(仅允许整数,KOF98 原生分辨率就是 320×224,TSR 原生图填 1);
  * `swap_p2`:P2 换色 LUT(逐色对);sprite_check 会打印每张 sheet 的主色榜辅助配色;
  * 缺姿势 → 该姿势回退程序绘制(混合呈现合法,渐进换装);`game/sprites/` 不存在 → 整体回退。
- 接入点:`assets.sprite()` 唯一调用口——先查 sprites 注册表(有则切格返回),否则走 terry.assemble;side(P1/P2)由 palette 颜色识别,用于选 LUT。
- `scripts/sprite_check.py`:①manifest 校验(边界/锚点/未注册姿势,报错带字段路径);②对全部 POSE 键输出"真图/回退"覆盖表;③渲染 contact sheet 预览 `scripts/out/sprite_preview.png` **给用户过目**(红线:预览图绝不 Read 进会话);④`--probe x.png` 按背景色探测网格,打印建议 strip 配置。退出码 0/1。

**A′. batch G 实测记录(FF1 原版 sheet → manifest)**
- 侦察工具链(PNG 零进会话):`scripts/sheet_ascii.py`(密度字符画+分带)/ 一次性探测脚本(四角众数抠底色→`mask.get_bounding_rects` 连通域=帧簇)/ 色标字符画(调色板颜色→字母,R=红衣 B=牛仔蓝 S=肤色 W=白 Y=金发——手臂和腿靠颜色秒辨);
- FF1 sheet 实况:552×2588、21 带、~65 人形帧(带内列隙≥3px 切帧),另含 logo/文字条/能量波弹体/换线弧特效等装饰;真图帧原生 ~100px 高与画布 1:1,`scale=1`;
- manifest:修复轮2 后 49 唯一帧 → 36 姿势 95 帧引用(其余 5 姿势 FF1 sheet 没有,回退程序绘制,清单见 §10.13);锚点规则:地面帧取底部 28% 不透明质心、腾空帧取全身质心、躺姿帧底行贴地取 COM-x(脚本计算,免手估),含 FX 图/拖尾的帧手调;`swap_p2` 五对(FF1 红系 (136,40,0)/(168,72,0)/(208,40,0)/(248,72,0)/(248,136,0) → 蓝系,火焰随换——P2 燃烧拳是蓝火,视觉自洽);
- 混排注意:真图画布宽度自适应(sprites.py `canvas_w=2*max(ax,w-ax)`),宽帧(燃烧拳突进 100px)不会被 56px 裁掉;ui 落点用 surface 实际尺寸,契约在真图下自然成立。

**B. 出招 timing 对齐(打击帧=判定帧;对程序绘制与真图同样生效)**
- 新纯函数 `attack_anim_frame(move, af, n)`(ui 层,零 pygame):
  * 姿势 n 帧按"起手…挥出×窗数,收招"布局:前 `n-1-k` 帧起手(k=判定窗数)、每窗对应 1 个挥出帧、末帧收招;
  * `af < windows[0].start` → 起手段循环;`af` 落在某窗 [start,end](到下一窗起)→ 该窗挥出帧(判定生效的拍显示最狠的姿势);过末窗 → 收招帧;发波招无窗,`projectile["start"]` 当打击时刻;
  * 替换 ui 里 `af % n`,修正"判定帧显示起手姿势"的反向 bug(现有素材立即受益)。
- 非攻击态保持全局帧驱动(ANIM_RATE 表),F 批再细化。

**C. 程序绘制中割升级(batch F,家族拆模块 + Workflow 并行)**
- `terry.py` 拆:PARTS + assemble/调色留门面,POSES 按家族拆 `terry_locomotion.py / terry_attacks.py / terry_specials.py / terry_reactions.py`(文件不重叠,可多代理并行);
- 加帧目标:walk 4→6、jump/jump_fall 各 2→3、run 分化、hit_high/fall 加帧、投技链 3 帧、12 招确保帧数 ≥ 三段式需求并与 B 映射核验;
- `poses.py` 计数同步(键名冻结不动);验收 = 像素采样(非空/帧数/部件色) + 全量回归。

### 11.3 测试计划(并入 §6 体系)
- `test_sprites.py`:**合成贴图自测**(测试内用 pygame 画小格 PNG + 临时 manifest → 加载/strip 切格/frames 切格/镜像/换色 LUT/坏 manifest 报错/无目录回退),零版权素材;
- `test_anim.py`:attack_anim_frame 全分支(单窗/双窗 power_dunk/发波/1帧姿势退化);打击帧落在 active 窗内的黄金断言;
- `sprite_check.py` 用合成图冒烟(退出码),真实图校验留给 batch G 用户参与。
- **考场环境契约(batch G 补)**:`tests/conftest.py` 把默认真图注册表钉死在"无图回退态"——机器上装没装 `game/sprites/terry/manifest.json` 不许影响考卷结果(否则同一套题在新鲜 clone 上绿、本机上红)。分工:test_art/test_anim 等经 assets.sprite 考**程序纸娃娃(回退路径)**,环境固定无真图;test_sprites 用注入注册表考**真图路径**(含 assets 集成端到端),自持 monkeypatch 不受影响。断言零改动,只固定考场环境。

---

## 附录A terry.json 招式数值基线(冻结版——测试对着这表写,调参走 batch D 文档变更)



> 格式:startup/active/recovery(帧);hs=命中硬直 bs=防御硬直;pb=推背(命中/防御,px);G=guard 层级;气=攻方涨气。**全部标注:推测,待验证。**

**普通技(按钮+站姿直接触发)**

| 招 | 帧数 | 伤害 | hs/bs | pb | G | 取消 | 气 |
|---|---|---|---|---|---|---|---|
| st_A 轻拳 | 3/2/8 | 30 | 12/8 | 10/12 | mid | 必杀,超杀 | 5 |
| st_B 轻脚 | 4/2/9 | 35 | 13/9 | 10/12 | mid | 必杀,超杀 | 5 |
| st_C 重拳 | 6/3/16 | 80 | 17/12 | 12/14 | mid | 必杀,超杀 | 8 |
| st_D 重脚 | 8/3/18 | 90 | 19/14 | 14/16 | mid | 无 | 8 |
| cr_A 蹲轻拳 | 4/2/8 | 30 | 12/8 | 10/12 | mid | 必杀,超杀 | 5 |
| cr_B 蹲轻脚 | 4/3/10 | 35 | 13/9 | 10/12 | **low** | 必杀,超杀 | 5 |
| cr_C 蹲重拳 | 6/3/14 | 75 | 16/12 | 12/14 | **low** | 必杀 | 8 |
| cr_D 扫堂腿 | 7/3/17 | 85 | 18/13 | 12/14 | **low**+SWEEP倒地 | 无 | 8 |
| j_A~j_D 空中技 | 5/至落地/— | 40/45/70/80 | 14/— | — | **high** | 无(落地lag 2帧) | 5/5/8/8 |

**必杀技**

| 招 | 指令 | 帧数 | 伤害 | hs/bs | 特性 | 削血 |
|---|---|---|---|---|---|---|
| 能量波A | ↓↘→+U | 14/—/26 | 60 | 16/11 | 弹速3.5px/f,同屏1发 | 8 |
| 能量波C | ↓↘→+O | 18/—/30 | 90 | 18/12 | 弹速4.5px/f | 12 |
| 燃烧拳A | ↓↙←+U | 11/8/20 | 75 | 17/12 | 突进vx6,出发1~4帧无敌 | 10 |
| 燃烧拳C | ↓↙←+O | 13/8/24 | 95 | 19/13 | 突进vx6.5 | 14 |
| 爆裂踢B | ↓↙←+I | 12/6/落地 | 70 | 16/11 | 弧线 vy0+5.5 vx4,**high 层级** | 8 |
| 爆裂踢D | ↓↙←+P | 14/6/落地 | 85 | 18/12 | vy0+6.0 vx4.5 | 10 |
| Power Dunk B | →↓↘+I | 8/6/24 | 55×2段 | 15/10 | 第二段 HARD 倒地 | 8×2 |
| Power Dunk D | →↓↘+P | 10/6/28 | 65×2段 | 16/11 | 同上 | 10×2 |
| 升龙撞A | ↓蓄↑+U | 6/12/28 | 110 | 18/13 | 上升vy+7,1~8帧无敌,对空 | 12 |
| 升龙撞C | ↓蓄↑+O | 8/14/32 | 140 | 20/14 | vy+8 | 16 |

**超必杀 / 投技**

| 招 | 指令 | 帧数 | 伤害 | 特性 | 耗气 |
|---|---|---|---|---|---|
| 能量喷泉 | ↓↘→↓↘→+U/O | 5/8/36 | 300 | 爆发框60宽×50高,1~3帧无敌,HARD 倒地,削血30 | **100(满槽)** |
| 前投 | 前+O 距≤28 | 3/2/20 | 110 | UNBLOCKABLE,前抛40px HARD;失败动作12帧 | — |
| 后投 | 后+O 距≤28 | 同上 | 110 | 后抛 | — |

## 附录B system.json 系统数值(冻结版)

| 项 | 值 | 项 | 值 |
|---|---|---|---|
| 视图/放大/舞台宽 | 320×224 / ×3 / 640 | 地面y / 开局x | 190 / 248 与 392 |
| 体力 / 回合时长 / 赛制 | 1000 / 60秒=3600帧 / 三局两胜 | hitstop(命中) | 普通11 必杀13 超杀20 投0 |
| 能量槽 | 100;命中普通+8/必杀+4,被击+5,防御+2 | hitstop(防御) | 9/10/14 |
| 走速 前/后 | 2.4 / 2.0 px/f | 跑速/制动 | 4.6 / 6帧 |
| 重力 | −0.35 px/f²(y 向上为正) | 大跳/小跳 vy0 | +8.0 / +4.0 |
| 前跳 vx / 小跳 vx / 后跳 vx | 2.8 / 1.8 / 2.4(向后,存正大小) | prejump / 空攻落地lag / 空跳落地 | 4帧 / 2帧 / 0帧 |
| 后撤(dash_back) | 22帧, vx3.0 vy0+3.5(向身后,存正大小) | 倒地起身 / 无敌帧 | 24帧 / 1~8 |
| 浮空弹起 vy | +3.0 | 连段伤害缩放 | 100/90/80/70/60/50…(50保底) |
| 受击框 站/蹲 | 30×92 / 30×58 | 推挤框 站/蹲 | 32×92 / 32×58 |
| 摄像机 | mid−160, clamp[0,320] | 角色界 | [cam+8, cam+312] |
| 输入:方向窗/按钮缓冲/DP窗/双QCF窗/蓄力 | 24 / 10 / 18 / 40 / 40帧 | 前冲判定 | 双击间隔≤8帧 |

## 附录C ai.json 权重表初版(入门档,seed 可配)

反应间隔 6 帧;距离档 远>160 / 中90~160 / 近<90;己方血<300 或贴角 → 保守档(block 权重 +0.15 后重归一;与 ai.json 冻结值同步,原"防御+50%后退归零"表述作废)。含按钮的动作计划(如超杀双QCF 8 帧)执行期间跳过决策点,防按钮帧被 6 帧节流掐掉。

| 档 | 动作权重 |
|---|---|
| 远 | 接近(走/跑)0.45,跳入0.15,能量波0.20,待机0.20 |
| 中 | 走近0.35,跑0.15,跳入0.15,爆裂踢0.05,能量波0.10,防御0.10,后撤0.10 |
| 近 | st_B 0.25,cr_B 0.15,必杀(燃烧拳)0.15,投技0.10,防御0.20,后撤0.10,跳0.05 |
| 反应 | 近距对手出招→55%防御;对手跳入(距离<120)→75% Power Dunk 迎击(升龙撞是蓄力招,反应窗口内出不来,勘误);气满近距→10%超杀 |

## 附录D POSE 注册表(键名冻结,帧姿势数 A5 定)

基础:idle walk run crouch stand_block crouch_block prejump jump jump_fall land air_hit fall knockdown wakeup hit_high hit_low dash_back throw_whiff throw_grab thrown win lose intro
普通技:st_A st_B st_C st_D cr_A cr_B cr_C cr_D j_A j_B j_C j_D
必杀:power_wave burn_knuckle crack_shoot rising_tackle power_dunk power_geyser
特效:proj_wave fx_geyser fx_hit fx_block

## 附录E 键位与出招表(玩家版,进 README)

- 移动:`W`跳 `S`下 `A`后 `D`前(双击`D`前冲,双击`A`后撤;轻点`W`小跳)
- 攻击:`U`轻拳 `I`轻脚 `O`重拳 `P`重脚
- 必杀:能量波 `S→D(斜着搓)→+U/O`;燃烧拳 `S→A→+U/O`;爆裂踢 `S→A→+I/P`;Power Dunk `D→S→斜下→+I/P`;升龙撞 `按住S约0.7秒→W+U/O`
- 超必杀(气满):能量喷泉 `S→D→S→D→+U/O`
- 投技:贴脸 `D+O`(前投)/ `A+O`(后投);防御:按住 `A`;`F1` 显示判定框;`Esc` 暂停
